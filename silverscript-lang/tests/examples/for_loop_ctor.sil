contract ForLoop(int start, int end) {
    int constant MIN_OUT = 1000;

    function check() {
        for(i, start, end) {
            require(tx.outputs[i].value >= MIN_OUT + i);
        }
    }
}
