pragma silverscript ^0.1.0;

contract P2PKH(bytes32 pkh) {
    function spend(pubkey pk, sig s) {
        bytes32 passedPkh = blake2b(pk);
        require(passedPkh == pkh);
        require(checkSig(s, pk));
    }
}
