pragma silverscript ^0.1.0;

contract R0Groth16(byte[32] image_id) {
    entrypoint function verify(byte[32] journal_hash, byte[] proof) {
        require(r0.g16.verify(journal_hash, proof, image_id));
    }
}
